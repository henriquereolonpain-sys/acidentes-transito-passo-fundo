# Dependências do app (Streamlit Cloud) — versões mínimas, sem pin exato
# para o pip resolver rápido e evitar backtracking no deploy.
streamlit>=1.40
folium>=0.18
streamlit-folium>=0.23
altair>=5.4
pandas>=2.2
duckdb>=1.1

# Usadas pelos scrapers/pipeline (não pelo app em si, mas leves)
requests>=2.32
beautifulsoup4>=4.12
geopy>=2.4
gdown>=5.1

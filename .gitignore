# Dados (regeneráveis pelo pipeline — não versionar)
data/*.duckdb
data/*.duckdb.wal
data/prf_cache/
data/*.log
# Exceção: o banco principal vai versionado para o deploy no Streamlit Cloud
# (snapshot ~10MB; atualizado manualmente quando quiser refrescar o demo)
!data/acidentes.duckdb

# Scripts de debug/inspeção descartáveis
check_*.py

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Ambientes virtuais
.venv/
venv/
env/

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Streamlit
.streamlit/secrets.toml

# Config local do Claude Code
.claude/

# Mockups de design exportados do Claude Design (referência local, não versionar)
*standalone*.html
